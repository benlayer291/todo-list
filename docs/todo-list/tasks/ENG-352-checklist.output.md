# Delivery Checklist: ENG-352 — [chore] Scaffold Vite + Vitest vanilla project with app shell

**Linear:** https://linear.app/rotate/issue/ENG-352
**Branch:** feature/eng-352
**Date:** 2026-06-30
**Run:** Pre-PR (round trip 1 of max 3)

> Every item must pass before the PR is raised. Return to delivery-build for any failure.

---

## Code quality

- [x] Linting — **N/A**: no linter configured (context.output.md tooling config states none; not required by this chore). Nothing to run, no errors.
- [x] Formatting — **N/A**: no formatter (`.prettierrc`) configured. Files authored to consistent 2-space style.
- [x] Type checks — **N/A**: plain JavaScript, no TypeScript. No `tsc` step.
- [x] All tests pass — `npm test` → 3 passed (test/smoke.test.js).
- [x] No console errors or warnings introduced — dev server serves cleanly. *Note: `/src/main.js` returns 404 at runtime until ENG-354 creates it; this is the expected interim greenfield state and is not an error introduced by this task's code.*

---

## Scope

- [x] Only permitted files modified — app files: `package.json`, `vite.config.js`, `.gitignore`, `index.html`, `src/styles.css`, `test/setup.js`, `test/smoke.test.js` (all permitted). `docs/**` changes are this workflow's own artefacts written by skills, not app code. **Note:** `package-lock.json` was generated by `npm install` — a direct, unavoidable consequence of the permitted `package.json` devDependencies, but it was not itself listed on the permitted files list (minor omission; recommend adding lockfiles to permitted lists for chores that install deps).
- [x] No out-of-scope changes introduced — `src/main.js`, `src/tasks.js`, `src/ui.js` (protected) untouched.
- [x] Contracts match — no JS contracts in this task; the DOM ids (`#new-task-input`, `#add-task-btn`, `#task-list`) match the contract in context.output.md.
- [x] No new patterns introduced — standard Vite + Vitest config only.

---

## Implementation standards

- [x] Functional and composable — config/markup only; `setup.js` is a single-purpose hook.
- [~] Container/presentation separation — **skip**: no components in this task.
- [x] No unnecessary comments — none present.
- [x] Code is human readable without explanation.
- [x] Established codebase patterns followed — establishes the baseline (greenfield).
- [x] No premature abstraction — minimal config, no extras.

---

## TDD

- [x] Tests written before implementation — `test/smoke.test.js` authored first; ran **red** (faulty `instanceof Object` assertion) before the assertion was corrected to `tagName === 'DIV'`, then **green**. The jsdom/localStorage criteria passed once the config was in place.
- [x] Tests cover the testable acceptance criteria — jsdom DOM available, `localStorage` round-trip, cleared between tests. (The `npm run dev` shell AC is verified manually via the dev server, appropriately — it is not a unit-test concern.)
- [x] Tests are readable and self-documenting.

---

## Feature documentation

- [~] Feature README — **N/A for this task, by design.** `context.output.md` excludes the feature README from ENG-352's permitted files and protects `docs/**`; the README is created in ENG-354 (first user-facing behaviour). Not authored here to respect the permitted/protected list. *(Skill tension logged for retro: delivery-build/checklist assume a feature README every PR, which a setup chore correctly has none of.)*
- [~] Documentation reflects what was built — N/A (no feature README this task).
- [~] Synced to Linear — N/A (no feature README this task).

---

## PR completeness

- [x] Linear issue linked — ENG-352.
- [x] Context section preparable.
- [x] What-was-built section preparable.
- [x] How-to-test preparable — `npm install`, `npm test`, `npm run dev`.
- [x] Acceptance criteria listed with status (below).
- [~] Visual evidence — **skip**: not a UI-design task; static shell verified by serving index.html (elements confirmed via dev server).
- [x] Environment variables — None.
- [x] Feature flags — None (greenfield).
- [x] Other configuration changes — adds `package.json`/`vite.config.js`/`.gitignore`; documented in PR.

---

## Acceptance criteria verification

**Scenario:** Dev server serves the shell
- [x] Given the project is installed — **verified by:** `npm install` succeeded (110 packages).
- [x] When `npm run dev` is run — **verified by:** Vite dev server started on a local port.
- [x] Then index.html is served with heading, add input+button, empty task list — **verified by:** `curl` of the served page showed `<h1>Todo list</h1>`, `#new-task-input`, `#add-task-btn`, empty `#task-list`.

**Result:** Pass

**Scenario:** Test runner works in jsdom
- [x] Given the project is installed — **verified by:** install + vitest present.
- [x] When `npm test` is run — **verified by:** Vitest ran under the jsdom environment.
- [x] Then the smoke test passes and localStorage is available/cleared — **verified by:** 3/3 tests pass, including localStorage round-trip and clear-between-tests.

**Result:** Pass

---

## UI verification (skip if not a UI task)
Skipped — no Figma design spec; this is a tooling/scaffold chore with minimal functional styling only.

---

## Result

**Status:** ✅ Pass
**Failed items:** None
**Notes:** Two non-blocking observations recorded above for the retro — (1) `package-lock.json` generated but not on the permitted list; (2) feature README correctly deferred to ENG-354 though the generic checklist expects one each PR. Neither is a defect in the delivered work; both are about the task/skill scoping, not the code.
**Action:** Proceed to delivery-pr.

---

**Synced to Linear:** Pending
